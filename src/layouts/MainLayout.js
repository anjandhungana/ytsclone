import { Nav } from "../components/partials/Nav";
import { AppRoutes } from "../routes/AppRoutes";

export const Main = () => {
  return (
    <main>
      <Nav />
      <AppRoutes />
    </main>
  );
};
